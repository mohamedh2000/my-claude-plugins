---
description: Spawns a new git worktree with a fresh Claude session running /prd
argument-hint: <task-name> - Name for the feature branch
---

# Spawn Worktree Command

Creates an isolated git worktree for feature development with its own Claude session.

Run the spawn skill with the provided task name.

$ARGUMENTS
